#include <stdio.h>
#include <stdlib.h>

int main(){
	 int pp;
	 int val,disc,t;
	 
	 printf("\nDigite 1 se voc� for funcion�rio e 0 se voc� for cliente:");
	 scanf("%d",&pp);
	 
	 printf("\nDigite o valor da compra:");
	 scanf("%d",val);
	 
	 if(pp == 1){
			t+=val*disc;

	 }
}